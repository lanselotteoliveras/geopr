% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geopr_preview.R
\name{geopr_preview}
\alias{geopr_preview}
\title{Interactive preview of a Puerto Rico geospatial layer}
\usage{
geopr_preview(
  layer,
  n = 500,
  wfs_url = "http://geoserver2.pr.gov/geoserver/pr_geodata/wfs",
  serviceVersion = "2.0.0",
  namespace = "pr_geodata",
  crs = NULL,
  bbox = NULL,
  cql_filter = NULL,
  backend = c("mapview", "leaflet"),
  color = "#E63946",
  alpha = 0.4,
  layer_name = NULL,
  use_cache = TRUE,
  timeout = 60
)
}
\arguments{
\item{layer}{Layer to preview. Accepts three forms:
\itemize{
\item \strong{Numeric}: the \code{layer_id} shown in \code{\link{geopr_list_layers}}
(e.g. \code{5}).
\item \strong{Character (short name)}: layer name without namespace
(e.g. \code{"g03_municipios"}).
\item \strong{Character (full name)}: with namespace included
(e.g. \code{"pr_geodata:g03_municipios"}).
\item \strong{\code{sf} object}: if you already have the layer downloaded, pass it
directly. In this case \code{n}, \code{bbox}, \code{cql_filter} and all connection
parameters are ignored.
}}

\item{n}{Integer. Maximum number of features to download from the server for
the preview. Default: \code{500}. Lowering this value speeds up loading;
increasing it shows more data but may take longer. Ignored if \code{layer}
is an \code{sf} object.}

\item{wfs_url}{Character. Base URL of the Puerto Rico WFS service.
Normally does not need to be changed.
Default: \code{"http://geoserver2.pr.gov/geoserver/pr_geodata/wfs"}.}

\item{serviceVersion}{Character. WFS protocol version to use.
Default: \code{"2.0.0"}. Change only if the server requires a different version
(e.g. \code{"1.1.0"}).}

\item{namespace}{Character. GeoServer namespace prefix for Puerto Rico.
Default: \code{"pr_geodata"}. Only needs to be changed if working with a
different GeoServer instance.}

\item{crs}{Integer. EPSG code to reproject data \strong{before} visualizing
(e.g. \code{32161} for Puerto Rico State Plane). If \code{NULL} (default), the
original layer CRS is used. For display, the function always reprojects
to WGS84 (EPSG:4326) internally.}

\item{bbox}{Bounding box to limit the download area. Accepts:
\itemize{
\item An \code{sf::st_bbox()} object.
\item A numeric vector of 4 elements: \code{c(xmin, ymin, xmax, ymax)}.
}
If \code{NULL} (default), downloads features from the full layer extent.}

\item{cql_filter}{Character. CQL filter to select a subset of features by
attribute. Uses OGC CQL standard syntax. Examples:
\itemize{
\item \code{"municipio = 'San Juan'"} -- features for San Juan only.
\item \code{"area_km2 > 100"} -- features with area greater than 100 km2.
}
If \code{NULL} (default), no filter is applied.}

\item{backend}{Character. Visualization engine. Options:
\itemize{
\item \code{"mapview"} (default): uses the \code{mapview} package, which provides
a richer interface with automatic controls, scale bar, and legend.
Requires \code{mapview} to be installed.
\item \code{"leaflet"}: uses \code{leaflet} directly. Automatically activated as
a fallback if \code{mapview} is not installed.
}
If neither is installed, the function throws an error with installation
instructions.}

\item{color}{Character. Main color for geometries on the map.
Accepts hex colors (e.g. \code{"#E63946"}) or R color names
(e.g. \code{"steelblue"}). Applied as fill for polygons and as the color for
points and lines. Default: \code{"#E63946"} (coral red).}

\item{alpha}{Numeric between \code{0} and \code{1}. Fill transparency level.
\code{0} = fully transparent; \code{1} = fully opaque. Default: \code{0.4}.
Only affects polygons and point circles.}

\item{layer_name}{Character. Label displayed in the map legend.
If \code{NULL} (default), derived automatically from the layer name
(e.g. \code{"g03_municipios"}).}

\item{use_cache}{Logical. If \code{TRUE} (default), reuses the cached layer list
if it was already fetched in the current session, avoiding a new server
call. Only applies when \code{layer} is numeric (\code{layer_id}).}

\item{timeout}{Integer. Maximum wait time in seconds for feature download.
Default: \code{60}. Increase this value if you have a slow connection or the
layer is very large.}
}
\value{
Invisibly, the downloaded \code{sf} object (or the one passed as \code{layer}).
The interactive map is rendered as a side effect in the RStudio
\strong{Viewer} panel or the default browser.
}
\description{
Generates an interactive map with a subset of features from a Puerto Rico
WFS layer. Designed for quick exploration: lets you inspect the shape,
extent, and attributes of a layer \strong{before} downloading it in full with
\code{\link[=geopr_get_layer]{geopr_get_layer()}}.
}
\section{Visualization backends}{

The function tries to use \code{mapview} by default as it offers a more complete
experience (layer controls, automatic popups, multiple basemaps).
If unavailable, it falls back to \code{leaflet}. Install either with:
\preformatted{
install.packages("mapview")   # recommended
install.packages("leaflet")   # alternative
}
}

\section{Automatic tooltips}{

When hovering over geometries, the map shows the value of the first
recognizable column (\code{nombre}, \code{name}, \code{municipio}, \code{descripcion}, \code{tipo},
etc.). If none of those columns exist, the first non-geometry column is used.
}

\examples{
\dontrun{
# 1. Quick preview by short layer name
geopr_preview("g03_municipios")

# 2. Preview with fewer features for faster loading
geopr_preview("g03_municipios", n = 78)  # PR has 78 municipalities

# 3. Using the layer_id from geopr_list_layers()
layers <- geopr_list_layers()
layers  # identify the desired layer_id
geopr_preview(5)

# 4. Pass an already-downloaded sf object directly
municipios <- geopr_get_layer("g03_municipios", all = TRUE)
geopr_preview(municipios, layer_name = "PR Municipalities")

# 5. Change colors and transparency
geopr_preview("g03_municipios", color = "#2A9D8F", alpha = 0.6)

# 6. Use leaflet as backend
geopr_preview("g03_municipios", backend = "leaflet")

# 7. Filter by attribute with CQL before previewing
geopr_preview("g03_barrios", cql_filter = "municipio = 'San Juan'", n = 200)

# 8. Limit by geographic area with bbox
geopr_preview("g03_barrios", bbox = c(-66.2, 18.3, -65.9, 18.5))

# 9. Capture the returned sf for later use
sf_preview <- geopr_preview("g03_municipios", n = 20)
nrow(sf_preview)  # number of downloaded features
}
}
\seealso{
\itemize{
\item \code{\link{geopr_list_layers}} to discover available layers and their IDs.
\item \code{\link{geopr_get_layer}} to download the full layer once identified.
\item \code{\link{geopr_save}} to save the downloaded layer to disk.
}
}
