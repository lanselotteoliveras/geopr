% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geopr_save.R
\name{geopr_save}
\alias{geopr_save}
\title{Save an sf layer from the Puerto Rico WFS to disk}
\usage{
geopr_save(
  x,
  layer_name = NULL,
  format = c("gpkg", "geojson", "shp", "fgdb", "csv"),
  dir = ".",
  filename = NULL,
  overwrite = FALSE,
  add_date = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{x}{An \code{sf} object to save.}

\item{layer_name}{Character. Layer name (e.g. \code{"g03_municipios"}).
Used to build the output filename. If \code{NULL}, attempts to extract it
from the object attribute or falls back to \code{"layer"}.}

\item{format}{Character. Output format. Options: \code{"gpkg"} (GeoPackage),
\code{"geojson"}, \code{"shp"} (Shapefile), \code{"fgdb"} (File Geodatabase), \code{"csv"}.
Default: \code{"gpkg"}.}

\item{dir}{Character. Output directory. Default: current working directory (\code{"."}).}

\item{filename}{Character. Custom filename (without extension).
If \code{NULL} (default), auto-generated as \code{"pr_geodata_{layer_name}_{date}"}.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites existing files. Default: \code{FALSE}.}

\item{add_date}{Logical. If \code{TRUE}, appends the current date to the filename.
Default: \code{TRUE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses messages from \code{sf::st_write()}. Default: \code{FALSE}.}

\item{...}{Additional arguments passed to \code{sf::st_write()}.}
}
\value{
Invisibly, the full path of the saved file.
}
\description{
Saves an \code{sf} object to disk with a smart filename based on the layer name
and the current date. Supports multiple geospatial formats via \code{sf::st_write()}.
}
\examples{
\dontrun{
municipios <- geopr_get_layer("g03_municipios", all = TRUE)

# Save as GeoPackage (default)
geopr_save(municipios, layer_name = "g03_municipios")

# Save as GeoJSON in a specific folder
geopr_save(municipios, layer_name = "g03_municipios",
           format = "geojson", dir = "data/")

# Custom filename
geopr_save(municipios, layer_name = "g03_municipios",
           filename = "municipios_pr", format = "gpkg")

# Without date in filename
geopr_save(municipios, layer_name = "g03_municipios", add_date = FALSE)
}
}
